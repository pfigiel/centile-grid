{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": [".expo/**", "dist/**"] },
    "lint": { "outputs": [] },
    "typecheck": { "outputs": [] }
  }
}

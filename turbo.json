{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "outputs": [".expo/**", "dist/**"] },
    "lint": { "outputs": [] },
    "typecheck": { "outputs": [] },
    "test": { "outputs": [] }
  }
}
